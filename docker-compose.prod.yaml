services:
  api-prod:
    container_name: backend_container_ppl
    env_file:
      - .env
    ports:
      - "8005:8000"
    build:
      context: ./backend/
      dockerfile: Dockerfile.backend.pro
    restart: unless-stopped
